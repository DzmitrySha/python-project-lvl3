"""IMG downloader module."""

# from page_loader.processes import (make_soup, make_name,
#                                    make_dir_path, write_bin_file)

tags = {
    "img": "src",
    "link": "href",
    # "script": "src",
}


def img_download(url: str, temp_folder=''):
    # soup = make_soup(url)
    pass
